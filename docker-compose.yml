version: "3.8"

services:
  bot:
    build: .
    ports:
      - "8001:8000"
    volumes:
      - bot-data:/data
      - ./dashboard:/app/dashboard
    env_file:
      - .env
    environment:
      - DATA_DIR=/data
    restart: unless-stopped

volumes:
  bot-data:
